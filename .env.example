# Backend (convo-api) environment variables.
# The placeholder server only reads PORT. As the backend grows, add secrets
# here (DB URLs, API keys, etc.), document them, and load them in server.js.
# The real .env is gitignored — never commit it.

PORT=3001
